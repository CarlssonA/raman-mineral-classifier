# ============================================================
# raman-mineral-classifier — Abhängigkeiten
# Python 3.10+
# Install: pip install -r requirements.txt
# ============================================================

# --- Daten & RRUFF-Zugriff ---
ramanspy>=0.1.2          # RRUFF-Daten laden: rp.datasets.rruff()
numpy>=1.24
pandas>=2.0

# --- Preprocessing & Signalverarbeitung ---
scipy>=1.11              # Peak-Detection, Interpolation
pybaselines>=1.1.0       # Baseline-Korrektur (ALS, SNIP, etc.)

# --- Machine Learning ---
scikit-learn>=1.4        # Random Forest, Cross-Validation, Metrics
tensorflow>=2.14         # 1D-CNN via Keras API
# Alternativ: torch>=2.0 + torchvision (auskommentiert)

# --- Visualisierung ---
matplotlib>=3.7
seaborn>=0.13

# --- Notebooks ---
jupyter>=1.0
jupyterlab>=4.0
ipywidgets>=8.0          # Interaktive Widgets in Notebooks

# --- Streamlit App ---
streamlit>=1.33
plotly>=5.18             # Interaktive Spektren-Plots in der App

# --- Hilfspakete ---
joblib>=1.3              # Modelle speichern/laden (sklearn-kompatibel)
tqdm>=4.66               # Fortschrittsbalken